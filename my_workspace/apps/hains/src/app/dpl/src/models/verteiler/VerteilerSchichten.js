import Schicht from '../apimodels/schicht';

class VerteilerSchichten extends Schicht {
  constructor(obj, appModel = false) {
    super(obj, appModel, false);
  }
}
export default VerteilerSchichten;
