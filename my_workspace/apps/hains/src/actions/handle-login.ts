'use server';
export const handleLogin = () => {
  console.log('Login called on the server');
  // const clientId = 'your_client_id';
  // const redirectUri = encodeURIComponent('http://localhost:3000/consent'); // Change to your consent page
  // const responseType = 'code';
  // const authUrl = `http://localhost:4000/oauth/authorize?client_id=${clientId}&redirect_uri=${redirectUri}&response_type=${responseType}`;
  //
  // window.location.href = authUrl; // Redirect to OAuth2 authorization endpoint
};
